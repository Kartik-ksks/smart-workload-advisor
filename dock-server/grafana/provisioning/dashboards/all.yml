apiVersion: 1

# tells grafana where to find the pre-created dashboards
providers:
  - name: 'default'
    folder: 'dashboards'
    type: 'file'
    options:
      path: '/etc/grafana/provisioning/dashboards'
